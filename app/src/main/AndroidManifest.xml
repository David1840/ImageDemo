<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.david.image">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.PrimaryColorActivity" />

        <activity android:name=".activity.ColorMatrixActivity" />

        <activity android:name=".activity.PixelsEffectActivity" />

        <activity android:name=".activity.ImageMatrixActivity" />

        <activity android:name=".activity.RoundRectXfermodActivity" />
        <activity android:name=".activity.BitmapShaderActivity" />
        <activity android:name=".activity.ReflectActivity" />
        <activity android:name=".activity.MeshActivity" />
    </application>

</manifest>